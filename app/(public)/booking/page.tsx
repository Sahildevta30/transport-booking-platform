import type { Metadata } from "next";
import Link from "next/link";
import { notFound,redirect } from "next/navigation";
import { Button } from "@/components/ui/button";
import { BookingForm } from "@/components/booking/booking-form";
import { createClient } from "@/lib/supabase/server";
export const metadata:Metadata={title:"Book trip"}; type Props={searchParams:Promise<{trip?:string;passengers?:string}>};
export default async function BookingPage({searchParams}:Props){const {trip:tripId,passengers:raw}=await searchParams;if(!tripId)notFound();const count=Math.min(50,Math.max(1,Number(raw)||1));const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect(`/login?next=${encodeURIComponent(`/booking?trip=${tripId}&passengers=${count}`)}`);const {data:trip}=await supabase.from("trips").select("id,route_id,vehicle_id,departure_at,arrival_at,status,base_price").eq("id",tripId).maybeSingle();if(!trip||trip.status!=="scheduled"||new Date(trip.departure_at)<=new Date())notFound();const [{data:route},{data:vehicle}]=await Promise.all([supabase.from("routes").select("id,name,origin_location_id,destination_location_id,distance_km").eq("id",trip.route_id).maybeSingle(),supabase.from("vehicles").select("id,label,registration_number,vehicle_type_id,status").eq("id",trip.vehicle_id).maybeSingle()]);if(!route||!vehicle||vehicle.status!=="active")notFound();const [{data:vt},{data:seats},{data:locations},{data:availability}]=await Promise.all([supabase.from("vehicle_types").select("booking_mode").eq("id",vehicle.vehicle_type_id).single(),supabase.from("vehicle_seats").select("id,seat_number,seat_type").eq("vehicle_id",vehicle.id).order("seat_number"),supabase.from("locations").select("id,name").in("id",[route.origin_location_id,route.destination_location_id]),supabase.rpc("trip_seat_availability",{p_trip_id:trip.id})]);if(!vt)notFound();const unavailableSeatIds=(availability??[]).filter(x=>x.availability!=="AVAILABLE").map(x=>x.seat_id);const names=new Map((locations??[]).map(l=>[l.id,l.name]));return <div className="mx-auto max-w-4xl space-y-6 px-4 py-10"><Button variant="ghost" asChild><Link href="/search">← Back to search</Link></Button><div><p className="text-sm text-muted-foreground">{route.name}</p><h1 className="text-3xl font-semibold">{names.get(route.origin_location_id)??"Origin"} → {names.get(route.destination_location_id)??"Destination"}</h1><p className="mt-2 text-muted-foreground">{new Date(trip.departure_at).toLocaleString()} · {vehicle.label} · Base price ₹{Number(trip.base_price).toFixed(2)}</p></div><BookingForm tripId={trip.id} seats={seats??[]} unavailableSeatIds={unavailableSeatIds} passengerCount={count} basePrice={Number(trip.base_price)} bookingMode={vt.booking_mode}/></div>}
