import type { Metadata } from "next";
import Link from "next/link";
import { ArrowRight, Bus, CarFront, Users } from "lucide-react";
import { Button } from "@/components/ui/button";
import { createClient } from "@/lib/supabase/server";
export const metadata:Metadata={title:"Vehicles"};
export default async function Page(){const supabase=await createClient();const{data:vehicles}=await supabase.from("vehicles").select("id,label,registration_number,seat_capacity,status").eq("status","active").order("label").limit(24);return <div className="mx-auto max-w-7xl px-4 py-14 sm:px-6 lg:px-8"><div className="max-w-3xl"><p className="text-xs font-black uppercase tracking-[.2em] text-primary">Choose your ride</p><h1 className="mt-3 text-4xl font-black tracking-tight sm:text-5xl">A ride for every kind of plan.</h1><p className="mt-3 text-muted-foreground">Explore active vehicles in the marketplace. Exact trip availability is confirmed through search.</p></div>{vehicles?.length?<div className="mt-10 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{vehicles.map(v=><article key={v.id} className="group overflow-hidden rounded-[1.75rem] border bg-card shadow-sm transition hover:-translate-y-1 hover:shadow-xl"><div className="grid min-h-40 place-items-center bg-[radial-gradient(circle_at_top,hsl(var(--primary)/.2),transparent_65%),hsl(var(--muted))]"><CarFront className="h-16 w-16 text-primary/70"/></div><div className="p-6"><div className="flex items-start justify-between gap-3"><div><h2 className="text-lg font-black">{v.label}</h2><p className="mt-1 text-xs text-muted-foreground">{v.registration_number}</p></div>{v.seat_capacity?<span className="flex items-center gap-1 rounded-full bg-muted px-2.5 py-1 text-xs font-semibold"><Users className="h-3.5 w-3.5"/>{v.seat_capacity}</span>:null}</div><Button asChild className="mt-5 w-full rounded-xl"><Link href="/search">Find a trip <ArrowRight className="ml-2 h-4 w-4"/></Link></Button></div></article>)}</div>:<div className="mt-10 rounded-[2rem] border border-dashed p-12 text-center"><Bus className="mx-auto h-8 w-8 text-primary"/><p className="mt-4 font-bold">Active vehicles will appear here when partner inventory is available.</p><Button asChild className="mt-5 rounded-xl"><Link href="/search">Search trips</Link></Button></div>}</div>;}
