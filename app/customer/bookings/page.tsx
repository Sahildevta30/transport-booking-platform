import type { Metadata } from "next";
import Link from "next/link";
import { redirect } from "next/navigation";
import { Button } from "@/components/ui/button";
import { createClient } from "@/lib/supabase/server";
export const metadata:Metadata={title:"My Bookings"};
export default async function CustomerBookingsPage(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect("/login?next=/customer/bookings");const {data:bookings,error}=await supabase.from("bookings").select("id,trip_id,booking_mode,status,passenger_count,amount,created_at").order("created_at",{ascending:false});return <div className="space-y-6"><div><h1 className="text-4xl font-black tracking-tight">My Bookings</h1><p className="mt-2 text-muted-foreground">Everything you booked, all in one place.</p></div>{error?<p className="rounded-lg border p-5 text-sm">Bookings could not be loaded.</p>:null}{!error&&!bookings?.length?<div className="rounded-[2rem] border border-dashed bg-card/60 p-12 text-center"><p className="font-medium">No bookings yet</p><Button asChild className="mt-4"><Link href="/search">Explore rides</Link></Button></div>:null}<div className="grid gap-4">{bookings?.map(b=><article key={b.id} className="rounded-[1.5rem] border bg-card p-6 shadow-sm transition duration-300 hover:-translate-y-0.5 hover:shadow-lg"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-lg font-black">Booking {b.id.slice(0,8).toUpperCase()}</p><p className="text-sm text-muted-foreground">{b.booking_mode.replaceAll("_"," ")} · {b.passenger_count} passenger(s) · ₹{Number(b.amount).toFixed(2)}</p><p className="mt-1 text-sm">Status: {b.status}</p></div><Button asChild variant="outline"><Link href={`/customer/bookings/${b.id}`}>View booking</Link></Button></div></article>)}</div></div>}
