import type {Metadata} from "next";
import {redirect} from "next/navigation";
import Link from "next/link";
import {createClient} from "@/lib/supabase/server";
export const metadata:Metadata={title:"Notifications"};
export default async function NotificationsPage(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect("/login?next=/customer/notifications");const {data:items,error}=await supabase.from("notifications").select("id,booking_id,channel,event_type,title,message,status,read_at,sent_at,created_at").order("created_at",{ascending:false}).limit(100);return <div className="space-y-6"><div><h1 className="text-3xl font-semibold">Notifications</h1><p className="mt-2 text-muted-foreground">Booking, payment, refund and trip updates.</p></div>{error?<p className="rounded-xl border p-5">Notifications could not be loaded.</p>:!items?.length?<div className="rounded-xl border bg-card p-8 text-center"><h2 className="font-semibold">No notifications yet</h2><p className="mt-2 text-sm text-muted-foreground">Important booking updates will appear here.</p></div>:<div className="space-y-3">{items.map(n=><article key={n.id} className="rounded-xl border bg-card p-5"><div className="flex flex-wrap items-center justify-between gap-2"><h2 className="font-semibold">{n.title}</h2><span className="text-xs text-muted-foreground">{new Date(n.created_at).toLocaleString()}</span></div><p className="mt-2 text-sm text-muted-foreground">{n.message}</p><div className="mt-3 flex flex-wrap gap-3 text-xs"><span>{n.channel.replaceAll("_"," ")}</span><span>{n.status}</span>{n.booking_id?<Link className="underline" href={`/customer/bookings/${n.booking_id}`}>View booking</Link>:null}</div></article>)}</div>}</div>}
