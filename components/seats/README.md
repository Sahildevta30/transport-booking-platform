# components/seats

Feature-specific UI components for the seats domain. Empty in Phase 1 by
design — components land here once the corresponding pages in app/ need
real, non-placeholder UI (Phase 2+).
