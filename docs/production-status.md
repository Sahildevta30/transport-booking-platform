# Production status

Email signup and profile creation have been verified against the live Supabase project. The previous localhost confirmation redirect was corrected in Supabase Auth URL Configuration. This branch adds the missing application-side PKCE callback and recovery completion route. Merge/deploy only after Vercel preview/build checks pass.
