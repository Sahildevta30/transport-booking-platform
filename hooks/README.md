# hooks/

Shared React hooks. Empty in Phase 1 — add hooks here once client
components need cross-cutting stateful logic (e.g. a future
`use-booking-flow`, `use-seat-map`). Keep hooks feature-agnostic;
feature-specific state belongs closer to its feature component.
