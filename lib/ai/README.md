# AI domain

`provider.ts` defines the `AiProvider` service boundary — natural
language in, structured search intent out, never a fabricated result.
See docs/architecture/ai-architecture.md for the full design and the
non-negotiable rule that AI never has direct database access.
