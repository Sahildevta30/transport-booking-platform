# Analytics domain

`events.ts` defines the typed event vocabulary and a no-op `trackEvent()`.
See docs/architecture/analytics-architecture.md for the full picture,
including what must never be tracked (secrets, payment credentials).
