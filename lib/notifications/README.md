# Notifications domain

Owns the notification abstraction (booking confirmations, reminders, cancellations). No delivery provider (SMS/WhatsApp/email) is wired up yet — see docs/architecture/backend-architecture.md.

Nothing here is implemented yet — this module exists so Phase 2 has a clear
home to build in, without restructuring the app around it later.
