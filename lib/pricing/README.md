# Pricing domain

Owns pricing rules and coupon logic. Pricing must always be computed server-side from real rules — never trust a client-supplied price.

Nothing here is implemented yet — this module exists so Phase 2 has a clear
home to build in, without restructuring the app around it later.
