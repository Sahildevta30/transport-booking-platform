# supabase/migrations

SQL migrations, applied via the Supabase CLI (`supabase migration new`,
`supabase db push`). Empty in Phase 1 — the schema described in
docs/database/domain-model.md is implemented here in Phase 2, as a set of
reviewable, incremental migrations (not one giant schema file).
