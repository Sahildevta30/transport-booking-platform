# utils/

Plain, framework-agnostic utility functions (formatting, date helpers,
etc.) that don't belong to a specific domain. `lib/utils.ts` already
holds the shadcn/ui `cn()` class-merging helper — general-purpose,
non-styling utilities go here instead as the app grows.
